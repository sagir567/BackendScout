# Official workflow: uv sync --extra dev
#
# Keep package dependencies centralized in pyproject.toml.
# This file gives pip users a fallback install command:
#   python -m pip install -r requirements.txt
-e .[dev]
